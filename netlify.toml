[build]
  publish = "dist"
  command = "npm run build"

# Proxy API requests to avoid CORS
[[redirects]]
  from = "/api/*"
  to = "https://server10-mu.vercel.app/api/:splat"
  status = 200
  force = true

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
